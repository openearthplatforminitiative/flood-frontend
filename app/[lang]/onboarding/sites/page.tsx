import { Box } from '@mui/material';

const Sites = () => {
  return (
    <Box sx={{ height: '100%', width: '100%', border: '2px solid black' }}>
      Sites page
    </Box>
  );
};

export default Sites;
